pipeline { 
 agent any 
  stages { 
     stage('deploy') { 
        steps {
          sh 'sudo kubectl apply -f adressbook-deployment.yaml'
          sh 'sudo kubectl apply -f addressbook-service.yaml'
        }
     }
   }
}
